import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    padding: 10,
    borderRadius: 5,
  },
  containerActivity: {
    flexDirection: "row",
  },
  containerText: {
    fontSize: 17,
    marginTop: 10,
  },
});
