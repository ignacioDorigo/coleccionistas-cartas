import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  overlay: {
    width: "90%",
    // height:'100%'
  },
  indicacion: {
    textAlign: "center",
    fontWeight: "bold",
    fontSize: 20,
    marginBottom:30,
  },
});
